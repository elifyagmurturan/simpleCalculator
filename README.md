This is a simple Node, Express & MongoDB Calculator App to practice. It performs addition, subtraction, division and multiplication operations.

To instal dependencies:
yarn install

To run:
node server.js